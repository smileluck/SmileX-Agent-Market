# ========================================#
# SmileX Agent Market 环境变量配置示例
# ========================================#

# 大模型配置
# 选择使用的大模型类型：openai 或 vllm
LLM_TYPE=openai

# OpenAI 配置（当 LLM_TYPE=openai 时生效）
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4

# VLLM 配置（当 LLM_TYPE=vllm 时生效）
# 本地部署的 VLLM API 地址
VLLM_API_BASE=http://localhost:8000/v1
# VLLM 本地部署一般不需要 API 密钥，可设置为任意值
VLLM_API_KEY=empty
# 本地部署的模型名称（如 qwen3、qwen3-72b 等）
VLLM_MODEL=qwen3

# 知乎爬虫配置
# 知乎登录 Cookie（可选，用于爬取需要登录的内容）
ZHIHU_COOKIE=your-zhihu-cookie
